{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "api": "json-server --watch db.json --port 3001 --watch"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.7.0",
    "nuxt": "^3.0.0"
  },
  "dependencies": {
    "@meforma/vue-toaster": "^1.3.0",
    "daisyui": "^2.47.0"
  }
}
